server {
    listen 443 ssl;
    listen [::]:443 ssl;
    include /etc/ssl/self-signed.conf;
    include /etc/ssl/ssl-params.conf;

    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;
}
